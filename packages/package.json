{
  "name": "@rd-bedrock-nova/packages",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "domain",
    "application",
    "infrastructure",
    "interfaces"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "rm -rf node_modules && npm run clean --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
